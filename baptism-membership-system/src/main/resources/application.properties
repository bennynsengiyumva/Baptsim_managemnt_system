server.port=8084

spring.application.name=baptism-membership-system
spring.datasource.url=jdbc:postgresql://localhost:5433/BMPMS
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD:benny123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Mail (SMTP) Configuration for 2FA
# Replace with your SMTP settings (e.g., Gmail app password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=nsengiyumvabenny1@gmail.com
spring.mail.password=${SMTP_PASSWORD:akcw yuvj qqjs cues}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Custom app configuration
app.frontend-url=http://localhost:5173
app.certificate-storage=uploads/certificates
app.profile-picture-storage=uploads/profile-pictures

# Backup configuration
backup.pg-dump-path=/Library/PostgreSQL/18/bin/pg_dump
backup.directory=backups

# Actuator configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always